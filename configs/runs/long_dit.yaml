training:
  data_dir: ./data/
  backbone: dit
  backbone_config: ./configs/dit.yaml
  embedding_dim: 384
  sigma_min: 0.0
  data_on_device: false
  batch_size: 128
  gradient_accumulation_steps: 1
  learning_rate: FILL_ME
  min_learning_rate: FILL_ME
  lr_warmup_iterations: 1000
  max_iterations: FILL_ME
  weight_decay: 0.0
  max_grad_norm: 1.0
  ema_decay: 0.9999
  device: cuda
  compile_model: true
  seed: 42
  log_every_iterations: 100
  val_every_iterations: FILL_ME
  val_max_iterations: 40
  snapshot_every_iterations: FILL_ME
  n_epochs: 100000
  checkpoint_dir: ./checkpoints/long_dit
  resume_from: null
  wandb:
    enabled: true
    project: piccolo-flow-matching
    run_name: long_dit
    mode: online
    resume_id: null
